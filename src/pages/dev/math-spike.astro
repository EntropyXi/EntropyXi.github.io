---
import BaseLayout from "@/layouts/BaseLayout.astro";
import MathJax from "@/components/content/MathJax.astro";

const spikeHtml = `
<p>行内公式：已知 $x^2 + y^2 = z^2$ 与 $\\mathbb{E}[\\boldsymbol{X}]$。</p>
<p>显示公式：$$\\int_0^1 x^2 \\, dx = \\frac{1}{3}$$</p>
<p>equation 环境：</p>
<p>$$\\begin{equation}\\operatorname{KL}(p \\parallel q) = \\sum_x p(x) \\log \\frac{p(x)}{q(x)} \\tag{1}\\end{equation}$$</p>
<p>aligned 环境：</p>
<p>$$\\begin{aligned} \\nabla_{\\theta} J(\\theta) &= \\mathbb{E}_{q}[\\nabla_{\\theta} \\log p(x, z; \\theta)] \\\\ &= \\lim_{N \\to \\infty} \\frac{1}{N} \\sum_{i=1}^N \\nabla_{\\theta} \\log p(x_i, z_i; \\theta) \\end{aligned}$$</p>
<p>align 环境：</p>
<p>$$\\begin{align} a &= b + c \\\\ c &= d \\tag{A} \\end{align}$$</p>
<p>cases 环境：</p>
<p>$$f(x) = \\begin{cases} x^2 & x \\ge 0 \\\\ -x^2 & x < 0 \\end{cases}$$</p>
<p>矩阵：</p>
<p>$$A = \\begin{bmatrix} a & b \\\\ c & d \\end{bmatrix}, \\quad B = \\begin{pmatrix} 1 & 2 \\\\ 3 & 4 \\end{pmatrix}$$</p>
<p>underbrace 与中文文本：</p>
<p>$$\\underbrace{x + x + \\cdots + x}_{n\\text{ 个}} = nx$$</p>
<p>长公式（应容器内横向滚动，不撑破页面）：</p>
<p>$$\\mathcal{L}(\\theta) = \\sum_{t=1}^{T} \\left\\| \\boldsymbol{x}_t - \\boldsymbol{\\mu}_{\\theta}(\\boldsymbol{x}_{t-1}, t) \\right\\|_2^2 + \\lambda \\sum_{i=1}^{M} \\left( \\frac{\\partial^2 \\Phi(\\boldsymbol{x}_i)}{\\partial \\boldsymbol{x}_i^2} \\right)^2 + \\int_0^T \\operatorname{tr}\\left( \\boldsymbol{\\Sigma}_{\\theta}(\\boldsymbol{x}_s, s) \\right) \\, ds$$</p>
`;
---

<BaseLayout
  title="数学渲染验证"
  description="MathJax 渲染能力验证页，仅用于迁移验收。"
>
  <h1 class="page-title">数学渲染验证</h1>
  <p>本页仅用于 Astro 迁移阶段的 MathJax 兼容性验证，不进入正式导航。</p>
  <div class="spike-body" set:html={spikeHtml} />
  <MathJax />
</BaseLayout>
