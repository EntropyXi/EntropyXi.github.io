{
  "name": "entropyxi-blog",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "preview": "astro preview",
    "preview:test": "node scripts/run-preview.mjs",
    "clean": "tsx scripts/clean.ts",
    "build": "npm run build:site && npm run build:search",
    "build:site": "astro build",
    "build:search": "pagefind --site dist",
    "check": "npm run format:check && npm run lint && npm run check:types && npm run audit:content && npm run audit:assets && npm run test:unit -- --run && npm run test:e2e && npm run audit:bundle && npm run audit:output",
    "check:types": "astro check",
    "capture:ui-baseline": "tsx scripts/capture-ui-baseline.ts",
    "capture:phase-3": "tsx scripts/capture-phase-3.ts",
    "capture:phase-8": "tsx scripts/capture-phase-8.ts",
    "audit:assets": "tsx scripts/audit-assets.ts",
    "audit:content": "tsx scripts/audit-content.ts",
    "audit:bundle": "tsx scripts/audit-bundle.ts",
    "audit:output": "tsx scripts/audit-output.ts",
    "test:unit": "vitest",
    "test:e2e": "playwright test",
    "lint": "eslint . && stylelint \"src/**/*.{css,astro}\"",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "smoke:production": "tsx scripts/check-production-urls.ts"
  },
  "dependencies": {
    "@astrojs/markdown-remark": "^7.2.4",
    "astro": "^7.2.4",
    "rehype-mathjax": "^7.1.0",
    "remark-math": "^6.0.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@axe-core/playwright": "^4.13.0",
    "@playwright/test": "^1.62.1",
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^26.3.0",
    "eslint": "^10.8.1",
    "eslint-plugin-astro": "^1.7.0",
    "pagefind": "^1.5.2",
    "postcss-html": "^2.0.0",
    "prettier": "^3.9.6",
    "prettier-plugin-astro": "^0.14.1",
    "stylelint": "^17.14.1",
    "stylelint-config-standard": "^40.0.0",
    "tsx": "^4.23.12",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.67.0",
    "vitest": "^4.1.11"
  },
  "engines": {
    "node": ">=22.12.0",
    "npm": ">=9.6.5"
  }
}
